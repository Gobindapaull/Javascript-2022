// loop
for (let i = 0; i < 5; i++) {
    console.log(`Index: ${i}`);
}

// map
const numbers: number[] = [1, 2, 3, 4, 5];
const doubled = numbers.map(num => num * 2);
console.log(doubled); // [ 2, 4, 6, 8, 10 ]
